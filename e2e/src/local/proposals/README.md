# Proposal spec tests

Spec tests of proposals the `testsuite` submodule does not carry yet, copied verbatim and
registered in `src/lib.rs` under the `src/local` folder.

| file | source |
|---|---|
| `wide-arithmetic/wide-arithmetic.wast` | <https://github.com/WebAssembly/wide-arithmetic/blob/main/test/core/wide-arithmetic.wast> |
