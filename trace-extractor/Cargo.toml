[package]
name = "trace-extractor"
version = "0.1.0"
edition = "2024"

[dependencies]
rwasm = { path = "..", package = "fluent-rwasm", default-features = false, features = ["tracing"] }

[features]
default = ["std"]
std = []
